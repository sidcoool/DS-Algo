//BINARY TREE
#include <iostream>
#include <vector>
using namespace std;

vector <int> Arr;

void Insert(int num)
{
	if(Arr.size()==0)
		Arr.push_back(num);

	
}

int main()
{
	Insert(10);
	rteurn 0;
}